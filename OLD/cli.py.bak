#!/usr/bin/env python3
# -*- coding: utf-8 -*-

"""
KoemojiAuto - コマンドラインインターフェース
シンプルなTUIでKoemojiAutoを操作
"""

import os
import time
import json
import subprocess
import utils
import sys
import io

# 文字化け対策：標準出力の文字コードを設定
sys.stdout = io.TextIOWrapper(sys.stdout.buffer, encoding='utf-8', errors='replace')

def clear_screen():
    """画面をクリア"""
    os.system('cls' if os.name == 'nt' else 'clear')
    # Windows環境では文字コードをUTF-8に設定
    if os.name == 'nt':
        os.system('chcp 65001 > nul')
